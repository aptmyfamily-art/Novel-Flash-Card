# SHVR Claude Agent Instructions

Use `AGENTS.md` as the canonical instruction file for this repository.

Critical rule: after any edit, verify the change, commit it, run `git push`, then run `clasp push -f` from `studentHomeVisit` before reporting completion.
