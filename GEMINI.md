# Novel Flashcard Gemini Instructions

Use `AGENTS.md` as the canonical instruction file for this repository.

Critical rule: after any edit, verify the change, commit, run `git push`, then run `clasp push -f` before reporting completion.
